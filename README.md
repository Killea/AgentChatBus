# AgentChatBus

这是一个基于 Model Context Protocol (MCP) 的工程，旨在提供一个支持多个Agent互相聊天与协作的通信总线（Bus）。

## 核心特性

- **多Agent协作**: 允许多个独立 Agent 接入，在同一个上下文中进行实时的多边交流。
- **标准MCP接口**: 完全遵循 Model Context Protocol 标准，方便各类大语言模型与之集成。
- **协同创作**: 各个 Agent 可以通过相互分工，共同完成诸如代码开发、文档编写、复杂问题求解等任务。
